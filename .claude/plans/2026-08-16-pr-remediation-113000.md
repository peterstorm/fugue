# PR Remediation Plan — Standalone Review Round 4

- **Branch:** `feat/f6-file-durable-runtime`
- **Date:** 2026-08-16
- **Review Run Directory:** `.claude/reviews/review-and-fix-runs/standalone-2026-08-16-111550-f6-file-durable-runtime`
- **Authoritative result:** `result.json` (digest `1dc3f3d561e1f641f5a6aa666fb727df3ab529d5eb5074b340d2fb3579e46d32`)
- **Scope:** kind `all`, changed-path union (63 frozen files: `packages/framework/src/**` in-scope modules + 21 test files + ADRs 0075–0080 + spec/plan docs)
- **Reviewers:** code-reviewer, silent-failure-hunter, pr-test-analyzer, type-design-analyzer, comment-analyzer, architecture-tech-lead (6/6 transcripts captured)
- **Refutation Panel:** 1 canonical critical → 3 lenses (`reproduction`, `intent`, `test-coverage`) → **3/3 upheld**, 0 refuted

## Findings summary

| Bucket | Count |
|---|---|
| Critical found (canonical, pre-panel) | 1 |
| Critical refuted | 0 |
| **Critical surviving (mandatory)** | **1** |
| Advisory found | 21 |
| Advisory accepted | 11 |
| Advisory deferred | 9 |
| Advisory dismissed | 1 |

## Surviving critical — mandatory fix

### C1 — ADR-0075 describes the pre-remediation silent-fold contract (comment-analyzer-1)

- **Claim:** ADR-0075 Decision line 41 ("A supplied namespace alone does not change the address") and Option 1 line 20 ("namespace-only options intentionally fold away") state the old silent-fold contract, but the shipped `compositeNodeKey` throws a contract violation when `namespace` is present without `index`/`attempt` (`assertNoNamespaceAlone`, `packages/framework/src/checkpoint/composite-node-key.ts`, pinned by `composite-node-key.test.ts:42`). The accepted 2026-08-14 remediation (plan `2026-08-14-pr-remediation-215348.md`, row `type-design-analyzer-1/5`) changed the behavior and updated code/module docs but left the accepted ADR describing the old contract.
- **Panel:** upheld on all three lenses (reproduction / intent / test-coverage). No test anywhere asserts the ADR's fold contract; the ADR is indexed `Accepted` with no superseding ADR.
- **Fix (doc-only, per repo ADR convention "immutable records; amendments recorded, not silently edited"):**
  1. Add an `## Amendment (2026-08-14)` section to `docs/adr/0075-…md` recording: namespace-only `SaveNodeOpts` are rejected as ambiguous caller error — `compositeNodeKey` throws; the file backend maps the throw to typed `checkpoint-write-failed`; pinned by `composite-node-key.test.ts` ("rejects a namespace supplied without index/attempt as ambiguous (AD-1)"); cites the 2026-08-14 remediation plan; notes Consequences' negative bullet ("A namespace alone cannot create a distinct entry…") already anticipates the rejection.
  2. Append an explicit pointer (not a rewrite) to the two stale statements: Decision bullet (line 41) and Option 1 cons (line 20).
  3. Update the `docs/adr/README.md` index row for 0075: `Accepted` → `Accepted (amended 2026-08-14)` (precedent: ADR 0009 "Accepted (amended by 0019)").

## Advisory dispositions (all 21, autonomous triage)

### Accepted (11) — fix in this round

| Id | Claim (short) | Fix |
|---|---|---|
| silent-failure-hunter-1 | `readCheckpoint` (`file/journal.ts:488–494`) returns `null` for an existing-but-unreadable `checkpoint.json` because `existsSync` swallows EACCES/ENOTDIR; contract promises a typed `FrameworkError` on fs failure | Replace exists-check with the sibling ENOENT-probe pattern: attempt `readFileSync`, treat only `errno === "ENOENT"` as absent, every other errno → typed `fsFailure("readCheckpoint", …)`. Add `probeErrorCode` import; drop now-unused `existsSync` import. Pin: EACCES case (self-skip when root, matching the existing `file-journal.test.ts:1317` accommodation) asserts `cache-error` kind + non-null; absent-file case still returns `null`. |
| silent-failure-hunter-2 | `acquireFileLock` 50-attempt timeout message names neither the blocking fence entries nor the reconcile state — a live tomb + live foreign owner yields a diagnostic pointing nowhere | On loop exit, enumerate `protocolEntries(lockPath, TOMB_PREFIX/BIRTH_PREFIX/REAP_PREFIX)` and append `; blocking fence entries: …` to the timeout message (matching the existing `last blocking owner probe` style). Pin: live tomb + live foreign owner → typed `cache-error(acquireFileLock)` whose message names the tomb entry. |
| silent-failure-hunter-3 | Code-constructed non-finite/non-representable clock rejections are unclassified → `retriabilityOf` labels a deterministically broken injected clock retriable, while analogous code-constructed invariant rejections are pinned `permanent` | Pass `"permanent"` at every code-constructed clock-rejection site: `journal.ts` `appendEvent` clock catch; `checkpointer.ts` `setMeta` (clock threw + non-representable) and `load` (clock threw + non-representable) — extend `checkpointerCacheError` with an optional `failureClass` param mirroring `cacheFailure`; `freshness-index.ts` `recordWrite` + `findConflict` (factory already accepts the class). Pin `failureClass === "permanent"` + `retriabilityOf === "non-retriable"` at each of the 7 sites. |
| pr-test-analyzer-1 | `fileOperationError` permanent-class inference branch is unpinned — a regression erasing the inner class would mislabel deterministic append rejections retriable with no test failing | Add unit pins in `file-boundary-error.test.ts` (inference preserves inner `permanent`; explicit class wins; non-cache-error reason stays unclassified) and extend the non-lossless `appendEvent` test (`file-journal.test.ts:~867`) to assert `failureClass === "permanent"` on both keyed and keyless paths. |
| pr-test-analyzer-2 | `file-freshness-index.test.ts` has zero `failureClass` assertions — `recordWrite`'s deterministic rejections are constructed `permanent` in source but unpinned | Extend the rejected-persisted-shape loop (~line 706) to assert `write.error.failureClass === "permanent"` (+ `retriabilityOf` non-retriable). |
| pr-test-analyzer-6 | `atomicWriteFile`'s non-string `contents` guard is untested (A08 pin covers only the path guard) | Pin: `atomicWriteFile(path, 42 as unknown as string)` throws typed `cache-error(atomicWriteFile)` whose message contains "contents must be a string". |
| pr-test-analyzer-7 | `FakeLlmClient` Map provider `req.system` fallback (`responses.get(req.model) ?? responses.get(req.system)`) is unpinned | Pin: Map keyed by a system-prompt string + request whose `model` is absent but `system` matches → ok with the mapped output. |
| type-design-analyzer-7 | `deepFreeze` (`file/job.ts:75`) iterates `Object.getOwnPropertyNames` — objects nested under symbol keys escape the freeze the snapshot contract describes | Iterate `Reflect.ownKeys` instead. Note: the FR-009 boundary rejects symbol-keyed state at both the factory seed and `updateData`, so the public `data` getter can never reach a symbol-keyed clone — the fix is pinned via a documented test-only seam (`__testDeepFreeze`, matching the repo's `__test*` convention) so a future snapshot source cannot silently weaken the "fully immutable" contract. |
| comment-analyzer-2 | `serializeFileEventRecord` doc (`event-record.ts:789–796`) re-enumerates the FR-009 rejection inventory inline, contradicting the module header's own pointer policy | Replace the parenthetical enumeration with a pointer to `assertLosslessEvent`'s contract, matching the two sites round-3 A25 already converted. |
| comment-analyzer-3 | Stale comment `state-machine/replay.ts:101–103` ("Cast the inner event type at the boundary…") sits above a `return` with no cast — the cast consolidated into `foldStep` in round 2 | Replace with a one-line pointer stating the envelope unwrap + `as E` cast lives in `foldStep`. |
| comment-analyzer-4 | `state-machine/index.ts:15` comment references transient work item "Gap-1" that appears nowhere else | Trim to the factual re-export note. |

### Deferred (9) — concrete evidence-based reasons

| Id | Reason |
|---|---|
| pr-test-analyzer-3 | `listEventFiles` statSync race branch: no deterministic seam exists on `createFileJournal` (no fs hooks on that path), and a real readdir/stat race is flaky-by-construction. The branch fails closed with a correctly typed transient `fsFailure`; reviewer self-rated 3/10. A production seam for an unobservable race is scope expansion for this PR. |
| pr-test-analyzer-4 | `journalCapacityError` call site inside `appendEvent`: reaching it requires a genuine 1,000,000-entry listing (multi-GB) or a new seam; the exported factory and its message are already pinned and the call site is two lines delegating to it. Reviewer self-rated 3/10; documented accepted limitation. |
| pr-test-analyzer-5 | Carried deferral A06 (`resume.ts:194` non-`FrameworkError` re-tag branch): unreachable through the shipped typed journal, which only throws typed `cache-error`s; a direct test requires a new journal injection seam. Round-3 plan (`2026-08-16-pr-remediation.md`) already documented the cost/benefit; nothing changed that reasoning. |
| type-design-analyzer-1 | `isFrameworkError` kind-only narrowing: the predicate guards boundaries that only ever throw fully-constructed typed values (all factories construct complete variants), so incomplete-payload admission is not reachable today. The practical fix — per-variant required-field checks — is a new validation layer over all 26 closed kinds, and the alternative (renaming to `hasFrameworkErrorKind`) is cosmetic. Design decision for a follow-up, not a defect fix. |
| type-design-analyzer-2 | `retriabilityOf` nominal `"retriable"` labels on graph-structural kinds: the nominal status is documented at the definition site (`errors.ts` comment) and at the single runtime consumer (`dag-runtime/retry-policy.ts:76–80`); those kinds are emitted at validation time and never routed into the retry loop. A third `Retriability` discriminant is a public type change to the error taxonomy and every exhaustive consumer — requires its own design record. |
| type-design-analyzer-3 | Unbranded `nodeId` on the `Checkpointer` port: branding it is a source-breaking change for every `saveNode`/`NodeState` consumer; the file adapter already re-validates every id with `isBoundaryId` before `join` (defense present and pinned), and the in-memory placeholder-brand dance is the documented cost of the port staying raw. Brand introduction is a versioned port revision. |
| type-design-analyzer-4 | `RunMeta.startedAt` raw `Date` with file/in-memory divergence: the divergence is inherent to the medium — persistence requires a representable instant (file rejects non-finite at `setMeta`), memory does not. Unifying on a validated `IsoDate` is a shared-port type change across every adapter and consumer; no defect versus either backend's documented contract. |
| type-design-analyzer-5 | `dedupKey` raw `string` on the `FileJournal`/`JobLike` surface: the reviewer's own first recommendation is to keep the port `string` (kernel fallback-keyer constraint); the FR-015 charset and AD-2 pipe-exclusion invariants are enforced and pinned at the file boundary. A widened surface alias is a versioned port decision, not a fix. |
| type-design-analyzer-8 | Plain-string lock lease: the module doc explicitly defers a branded lease ("may be introduced in a future API revision; deliberately not part of this internal primitive's current surface"). A cross-path token mismatch makes release a silent no-op (safe direction — the lock is not freed). Branding is a breaking type change with no current defect to fix. |

### Dismissed (1)

| Id | Reason |
|---|---|
| type-design-analyzer-6 | `checkpoint-write-failed` dual representation (branded `runId`/`nodeId` + additive `invalidRunId`/`invalidNodeId`): the reviewer's own verdict is "acceptable as a source-compat pin"; the construction sites are real (`checkpointer-codec.ts:112`, `file/checkpointer.ts:~160`) and the truthful-placeholder behavior is pinned by the file-checkpointer suite (invalid-raw-nodeId case from the 2026-08-14 remediation). Collapsing the shape is breaking for consumers reading the additive fields; no defect. |

## Changed files (planned)

Production:
- `docs/adr/0075-composite-checkpoint-node-key-encoding-with-canonical-folding.md` (C1)
- `docs/adr/README.md` (C1 index row)
- `packages/framework/src/file/journal.ts` (A-0 readCheckpoint; A-2 appendEvent clock class)
- `packages/framework/src/file/atomic.ts` (A-1 timeout diagnostic)
- `packages/framework/src/file/checkpointer.ts` (A-2 setMeta/load clock classes + factory param)
- `packages/framework/src/file/freshness-index.ts` (A-2 recordWrite/findConflict clock classes)
- `packages/framework/src/file/job.ts` (A-7 deepFreeze)
- `packages/framework/src/file/event-record.ts` (A-8 doc pointer)
- `packages/framework/src/state-machine/replay.ts` (A-9 comment)
- `packages/framework/src/state-machine/index.ts` (A-10 comment)

Tests:
- `packages/framework/src/__tests__/file-journal.test.ts` (A-0, A-2, A-3 pins)
- `packages/framework/src/__tests__/file-atomic.test.ts` (A-1, A-5 pins)
- `packages/framework/src/__tests__/file-checkpointer.test.ts` (A-2 pins)
- `packages/framework/src/__tests__/file-freshness-index.test.ts` (A-2, A-4 pins)
- `packages/framework/src/__tests__/file-boundary-error.test.ts` (A-3 unit pins)
- `packages/framework/src/__tests__/llm-fake-client.test.ts` (A-6 pin)
- `packages/framework/src/__tests__/file-job.test.ts` (A-7 pin)

## Support paths (outside reviewed scope)

- `.claude/plans/2026-08-16-pr-remediation-113000.md` (this plan)

## Validation commands

```bash
cd /home/peterstorm/dev/agentic/fugue
bun run typecheck
bun test packages/framework
```

Iterate until both are green; no staging/commit before validation passes.
