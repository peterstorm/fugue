# PR Remediation Plan — Round 9

**Date:** 2026-07-05
**Branch:** feat/deterministic-core-phase-b
**Findings:** 1 critical, 14 advisory (5-agent review, determinism-focused)

## Critical Fixes

### Fix 1: Bare catch on readdir can silently wipe registry.json
- **Source:** silent-failure-hunter
- **File:** packages/framework/src/cli/prompts.ts:63-67 (consequence at :127-134)
- **Issue:** EACCES/EIO/ENOTDIR on readdir treated as "no prompts dir" → runPromptsSync marks all prompts removed, rewrites registry to {}, returns ok:true.
- **Fix:** Fold only ENOENT to empty map; rethrow everything else (callers' registry-unreadable envelope catches it). Test with a non-ENOENT failure.

## Advisory Fixes

### Fix 2: readRegistry folds non-ENOENT read failures into "no registry"
- **File:** prompts.ts:77-81
- **Fix:** Fold only ENOENT to {}; rethrow otherwise.

### Fix 3: Unvalidated cast after JSON.parse in readRegistry
- **File:** prompts.ts:82-86
- **Fix:** Per-entry shape validation (version/hash strings, valid status); throw naming the offending key.

### Fix 4: runPromptsSync writeFile escapes stdout-JSON contract
- **File:** prompts.ts:132 (via bin/fugue.ts:108)
- **Fix:** try/catch returning `{ ok:false, problems: ["registry write failed: …"] }` with stack, like runNew.

### Fix 5: registry.json byte order environment-dependent, two writers disagree
- **Source:** architecture (determinism)
- **File:** prompts.ts:114 (localeCompare), prompts.ts:38 (serializeRegistry), new.ts:524-529
- **Fix:** Canonical codepoint sort inside serializeRegistry; delete call-site sort; sort readPromptFiles entries the same way. Golden byte-stability test.

### Fix 6: --force regeneration not idempotent — stale prompt files break prompts check
- **File:** new.ts:486-530
- **Fix:** Under --force, reconcile tool-owned prompts/ dir against scaffold.prompts (remove stale entries). Fixed-point test: regen(a) then regen(b, force) ≡ fresh regen(b).

### Fix 7: Compose LLM turns run at default temperature
- **File:** src/llm/anthropic-client.ts:127-135, compose.ts:413-419
- **Fix:** Add optional temperature to LlmRequest; adapter threads it; compose pins temperature: 0 on structured turns.

### Fix 8: Generated bytes depend on nodes array order
- **File:** authored-codegen.ts:342-345, new.ts:517
- **Fix:** Canonicalize dag.nodes to structureOrder after parse (pure, total — every node referenced exactly once); codegen iterates one order. Permutation property test on scaffold bytes.

### Fix 9: ImportedDagFile success arm carries unparsed record — consumers re-cast
- **File:** lint.ts:28/99, describe.ts:31, lint.ts:139
- **Fix:** Success arm carries `dag: DagDef` (single cast at the single checked site) + raw registration record.

### Fix 10: describe.ts meta ad-hoc cast + dead cast
- **File:** describe.ts:36,38
- **Fix:** Small parsed registration-meta helper with defaults; remove dead `dag.id as string`.

### Fix 11: RouterCase label KEBAB-validated but not branded
- **File:** authored.ts:257
- **Fix:** Parse into Kebab brand, matching team's treatment.

### Fix 12: shape as Shape after non-narrowing includes
- **File:** new.ts:201,224
- **Fix:** `parseShape(raw): Shape | null` smart constructor.

### Fixes 13–17: Test gaps (pr-test-analyzer)
- authored-codegen.ts:103 — assert exactly one `confidence` occurrence for router-llm (skip branch of withConfidence)
- visualize.ts:133 — warnings threading through runVisualize ok-arm (route schema-warning fixture)
- new.ts:449 — non-empty NewResult.warnings threaded to machine-readable outcome
- authored.ts:575 — issuesToProblems empty-path branch (non-object root)
- compose.ts:190 / new.ts:118 — pin duplicate-flag last-wins behavior

## Validation Commands
```bash
cd packages/framework && bunx tsc --noEmit && bun test
```
