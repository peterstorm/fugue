{
  "name": "@fuguejs/examples",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Golden, lint-tested DAG examples — one per topology + capability pattern. The canonical copy-paste source for authoring Fugue DAGs.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@fuguejs/framework": "workspace:*",
    "@fuguejs/document-source": "workspace:*"
  },
  "devDependencies": {
    "@fuguejs/host": "workspace:*",
    "@fuguejs/pg": "workspace:*",
    "@types/bun": "latest",
    "zod": "^4.3.6"
  },
  "peerDependencies": {
    "zod": "^4.3.6"
  }
}
